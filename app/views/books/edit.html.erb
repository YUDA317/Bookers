<h1>Editing Book</h1>

<%= form_with model: @book do |f| %>

  <span>Title</span>
  <%= f.text_field :title %>

  <span>Body</span>
  <%= f.text_area :body %>
  <%= f.submit 'Update Book' %>

<%= link_to "Show", book_path(@book.id) %>
<%= link_to "Back", '/books' %>

<% end %>